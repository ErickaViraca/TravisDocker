language: java
jdk:
- oraclejdk8
sudo: required

services: 
  - docker

before_install:
  - docker info
  - docker run -i -t erickaviraca/travisdocker2:latest /bin/bash

script: gradle check
branches:
only: develop

notifications:
  email: true
  recipients:
  - Ericka.Viraca@fundacion-jala.org
